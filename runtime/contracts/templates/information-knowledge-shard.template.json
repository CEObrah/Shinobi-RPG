{
  "schema": "file-template",
  "template_id": "template.information-knowledge-shard",
  "target_schema": "information-knowledge-shard",
  "source_schema": "game/schemas/information-knowledge-shard.schema.json",
  "scope": "mutable_state",
  "current_directories": [
    "state/reg/information"
  ],
  "unknown_key_policy": "reject",
  "required_top_level_keys": [
    "bucket",
    "claim_refs",
    "holder_hash",
    "holder_ref",
    "schema"
  ],
  "object_contracts": {
    "": {
      "mode": "closed",
      "allowed_keys": [
        "schema",
        "holder_ref",
        "holder_hash",
        "bucket",
        "claim_refs"
      ],
      "canonical_order": [
        "schema",
        "holder_ref",
        "holder_hash",
        "bucket",
        "claim_refs"
      ]
    }
  },
  "type_contracts": {
    "": [
      "object"
    ],
    "/schema": [
      "string"
    ],
    "/holder_ref": [
      "string"
    ],
    "/holder_hash": [
      "string"
    ],
    "/bucket": [
      "string"
    ],
    "/claim_refs": [
      "array"
    ],
    "/claim_refs/*": [
      "string"
    ]
  },
  "array_contracts": {
    "/claim_refs": {
      "item_types": [
        "string"
      ]
    }
  },
  "writing_rules": [
    "Load this template before creating or structurally editing a target owner.",
    "Exact information authority is sharded deterministically; the root routing index is never claim/delivery/knowledge truth.",
    "World truth is not knowledge."
  ]
}
