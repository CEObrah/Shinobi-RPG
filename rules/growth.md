# Growth and Training Mechanical Authority

Use `data/mechanics/training.json` and `data/development/model.json`. Exact, individual-lite, and unit aggregate resolution all use representation efficiency 1.0. Training consumes real instructor/facility/equipment capacity. High rank requires field/command experience and institutional gates. A whole unit never receives institutional promotion merely because time passed; qualified individuals/subsets advance while headcount conserves.
