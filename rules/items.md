# Items

`data/items/index.json` and its referenced item files are the authoritative item registry. Mechanically identical items share one catalog definition; instances reference the catalog ID.

Training changes how well a person uses an item. It does not rewrite the item's base mechanics.

Every active loadout and inventory reference must resolve before canonical play.
