Before maintaining this repository, read `RUNTIME.md` and `REPOSITORY_MAP.md`. For narration behavior, read `VOICE.md`.

Do not treat migration history, tests, schemas, scripts, examples, indexes, neighboring owners, chat memory, or model inference as campaign truth or structural authority. Preserve current authority, run validation after structural changes, and never claim persistence succeeded unless the actual write succeeded.

For every mutable owner creation or structural write, resolve the exact registered structural template through `data/runtime/template-index.json`, the fact-free blank owner skeleton through `data/runtime/blank-owner-index.json`, and at least one relevant update contract through `data/runtime/system-contract-index.json`. Create new owners from the blank skeleton and fill only fields and types allowed by the template under that contract. A missing or ambiguous template, blank skeleton, or update contract stops persistence; never improvise structure. Adding, removing, renaming, or changing the shape/type of a field is maintenance: update the formal schema, structural template, blank skeleton, and affected update contracts first, then run the full validator stack before gameplay may write that structure. Blank skeletons define shape only and never supply campaign facts, defaults, capability, resources, relationships, knowledge, authority, or intent.

OOC discussion and preview/design work are not campaign state. Preserve player intent boundaries during every migration.