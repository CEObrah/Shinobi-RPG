{
  "schema": "file-template",
  "template_id": "template.jianghu-market-state-1.0",
  "target_schema": "jianghu-market-state-1.0",
  "source_schema": null,
  "scope": "mutable_state",
  "current_directories": [
    "state/martial-world/markets"
  ],
  "unknown_key_policy": "reject",
  "required_top_level_keys": [
    "schema",
    "region_id",
    "stock",
    "cycles_settled",
    "cash_pool"
  ],
  "object_contracts": {
    "": {
      "mode": "closed",
      "allowed_keys": [
        "schema",
        "region_id",
        "stock",
        "cycles_settled",
        "cash_pool"
      ],
      "canonical_order": [
        "schema",
        "region_id",
        "stock",
        "cycles_settled",
        "cash_pool"
      ]
    },
    "/stock": {
      "mode": "open_map"
    }
  },
  "type_contracts": {
    "": [
      "object"
    ],
    "/schema": [
      "string"
    ],
    "/region_id": [
      "string"
    ],
    "/stock": [
      "object"
    ],
    "/stock/*": [
      "integer"
    ],
    "/cycles_settled": [
      "integer"
    ],
    "/cash_pool": [
      "integer"
    ]
  },
  "array_contracts": {},
  "writing_rules": [
    "Load this template before creating or structurally editing a target owner.",
    "Do not add an unregistered field. A new field requires maintenance-time template/schema revision first.",
    "Dynamic/open maps permit new IDs only; each value must still satisfy the wildcard value contract.",
    "Indexes/caches are rebuilt from authority and never used to bless new authoritative fields.",
    "Canonical key order is a writing convention only; semantic validation does not depend on JSON object order."
  ]
}
