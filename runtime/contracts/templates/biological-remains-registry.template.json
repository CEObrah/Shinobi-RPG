{
  "schema": "file-template",
  "template_id": "template.biological-remains-registry",
  "target_schema": "biological-remains-registry",
  "source_schema": "game/schemas/biological-remains-registry.schema.json",
  "scope": "mutable_state",
  "current_directories": ["state/reg"],
  "unknown_key_policy": "reject",
  "required_top_level_keys": ["schema", "records"],
  "object_contracts": {
    "": {
      "mode": "closed",
      "allowed_keys": ["schema", "records"],
      "canonical_order": ["schema", "records"]
    },
    "/records": {"mode": "open_map"},
    "/records/*": {
      "mode": "closed",
      "allowed_keys": ["id", "subject_ref", "source_mission_ref", "custodian_ref", "place_ref", "status", "recovered_at", "evidence_refs", "classification"],
      "canonical_order": ["id", "subject_ref", "source_mission_ref", "custodian_ref", "place_ref", "status", "recovered_at", "evidence_refs", "classification"]
    }
  },
  "type_contracts": {},
  "array_contracts": {
    "/records/*/evidence_refs": {"item_types": ["string"], "min_items": 1, "unique": true}
  },
  "writing_rules": []
}
