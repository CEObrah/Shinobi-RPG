# Repository Development

When a repository connector is explicitly used, treat it as a bounded remote surface rather than a bulk workstation. Preserve one authoritative source, avoid manual reconstruction of large state, and verify the exact delivery tier. Never expose credentials or secrets.
